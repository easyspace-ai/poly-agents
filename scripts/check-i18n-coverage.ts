#!/usr/bin/env bun
/**
 * Placeholder i18n coverage check — full key coverage tooling can be added later.
 * Exits 0 so `bun run validate:ci` does not fail on a missing historical script.
 */
console.log('i18n coverage check: skipped (no-op)')
process.exit(0)
